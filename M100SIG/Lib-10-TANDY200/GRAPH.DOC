GRAPH.200 creates cartesian graphs from multiple RAM and Disc files.
(Sorry, DOS not included).
Data must be entered in a .DO file in the form X,Y [ENTER], for example:
		1.1,2.5
		7,7
		312,28
		10.9,11.2
		.00123,.2134
The program then generates a pixel map of the data.
The following options are then displayed:

F1: LIST F2:GRAPH F4:RUN F6:SCALE F8:MENU

F1 will print data points.
F2 will dump graph to a Dot Matrix Printer.
F4 will restart the program.
F8 will exit to MENU.
F6 allows the user to change the graph's format.
	X-MIN changes the minimum X value.
	X-MAX changes the maximum X value.
	Y-MIN changes the minimum Y value.
	Y-MAX changes the maximum Y value.
	X-TICKS changes the number of ticks on the horizontal axis.
	Y-TICKS changes the number of ticks on the vertical axis.
	HORIZONTAL changes the length of the horizontal axis (1 to 50).
	VERTICAL changes the length of the vertical axis (1 to 50).

The printer codes are listed for a Tandy Dot Matrix Printer.
Other printers can be used by changing lines 401-412:
	401 P1$=compressed font (12 cpi).
	402 P2$=half line feed (1/12").
	403 P3$=carraige return with NO line feed.
	404 P4$=carraige return with line feed.
	405 P5$=standard font.
	406 P6$=full line feed.
	407 CR$=execute a carraige return.
	408 LF$=execute a line feed.
	409 C1$=symbol for a vertical line.
	410 C2$=symbol for a vertical tick.
	411 C3$=symbol for a horizontal line.
	412 C4$=symbol for a horizontal tick.

Any problems, drop me a note.
Fred Brown [73267,2450]

